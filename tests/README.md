# tests

Integration-style test harnesses:

- `failure-scenarios/` for retry/recovery/failure behavior
- `leader-failover/` for lease election and scheduler handoff behavior
- `redis-coordination/` for lock expiry and partition behavior in coordination paths
