# shared-core tests

Chronos uses `doctest` as the unit test framework baseline and `ctest` as the
test runner entrypoint.

Shared domain and utility logic should place unit tests in this directory as the
first implementation work lands.
